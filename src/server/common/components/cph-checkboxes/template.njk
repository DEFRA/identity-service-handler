{% from "govuk/components/checkboxes/macro.njk" import govukCheckboxes %}

{{ govukCheckboxes({
  idPrefix: "cphs",
  name: "cphs",
  fieldset: {
    legend: {
      text: params.caption,
      classes: "govuk-visually-hidden",
      isPageHeading: false
    }
  },
  hint: {
    text: "Select all that apply"
  },
  classes: "govuk-!-margin-bottom-6",
  errorMessage: ({ text: params.errorMessage } if params.errorMessage),
  items: params.checkboxItems
}) }}