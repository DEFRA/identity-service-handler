{% extends 'layouts/page.njk' %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <a href="/account/delegations" class="govuk-back-link">Back</a>

      <h1 class="govuk-heading-l">{{ heading }}</h1>

      <p class="govuk-body">{{ delegated_user_email }}</p>

      <form method="post" action="/account/delegations/{{ delegated_user_id }}/delete">
        <input type="hidden" name="_csrf" value="{{ _csrf }}">

        {{ govukButton({
          text: "Continue"
        }) }}
      </form>

      <p class="govuk-body">
        <a href="/account/delegations" class="govuk-link">Cancel</a>
      </p>
    </div>
  </div>
{% endblock %}
